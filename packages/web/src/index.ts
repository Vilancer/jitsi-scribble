// Private placeholder — Phase 4 fills this in with the real Vite-built DOM
// renderer (SVG + CSS opacity fade, injected via custom-config.js). "private":
// true in package.json is temporary and removed once real content ships.
export {};
