// Private placeholder — Phase 7 fills this in with the Android
// SYSTEM_ALERT_WINDOW overlay Expo module + config plugin (Android-only;
// iOS gets the AVPictureInPictureVideoCallViewController composite instead,
// per the project's documented platform-asymmetry decision). "private": true
// in package.json is temporary and removed once real content ships.
export {};
