// Private placeholder — Phase 5 fills this in with the real RN overlay
// (react-native-svg + gesture-handler + Reanimated). "private": true in
// package.json is temporary and removed once real content ships.
export {};
