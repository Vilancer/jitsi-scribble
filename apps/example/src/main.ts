import { __PLACEHOLDER__ } from '@jitsi-scribble/protocol';

if (__PLACEHOLDER__ !== true) {
  throw new Error('protocol package did not resolve');
}

console.log('example: protocol package resolved OK');
