# Generated by scripts/build-test-transport.mjs (playwright globalSetup) —
# rebuilt before every suite run, never committed.
tests/fixtures/test-transport.iife.js
test-results/
playwright-report/
# Staging JWT secret for e2e auth — never commit (user rotates it out-of-band).
.env.e2e
# Generated portrait fake-camera fixture (see global-setup.ts) — raw y4m, never committed.
tests/fixtures/portrait.y4m
